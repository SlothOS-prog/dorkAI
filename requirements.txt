# Сторонние зависимости проекта dorkAI.
# Установка одной командой:  python -m pip install -r requirements.txt

# httpx — современный сверхбыстрый HTTP-клиент (замена requests) для запросов к ИИ-API
httpx>=0.27,<1.0

# python-dotenv — удобная загрузка переменных окружения из файла .env (для хранения API-ключа)
python-dotenv>=1.0,<2.0

# Примечание: tkinter (графический интерфейс) входит в стандартную поставку Python.
