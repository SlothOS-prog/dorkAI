"""
Пакет dorkAI — бэкенд-ядро генератора Google Dorks на базе ИИ.

Слои пакета (снизу вверх):
    config         — настройки из переменных окружения + сохранение API-ключа;
    prompts        — системный промпт (инструкция для ИИ);
    ai_client      — HTTP-клиент к OpenAI-совместимому API (httpx, ретраи);
    dork_generator — сервисный слой: текст пользователя -> список Dork;
    gui / cli      — тонкие оболочки над одним и тем же ядром.
"""

# Версия пакета — хранится в одном месте, чтобы легко обновлять
__version__ = "1.0.0"

# Что доступно при импорте вида `from dorkai import ...`
__all__ = ["Settings", "DorkGenerator"]
